# PRODUCTION ENVIRONMENT CONFIGURATION
# These values MUST be set before deploying to production
# DO NOT commit real credentials to version control

# Supabase Production Project URL
# Replace with your actual production Supabase URL
VITE_SUPABASE_URL=https://[PRODUCTION_PROJECT_ID].supabase.co

# Supabase Production Anon Key
# Replace with your actual production Supabase anon key
VITE_SUPABASE_ANON_KEY=[PRODUCTION_ANON_KEY]

# Application Environment
VITE_APP_ENV=production

# Disable debug logging in production
VITE_DEBUG=false

# Production Safety Flags (MUST be enabled)
VITE_USE_SUPABASE=true
VITE_ENFORCE_RLS=true
VITE_REQUIRE_AUTH=true
